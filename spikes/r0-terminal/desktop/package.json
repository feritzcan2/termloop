{
  "name": "@termloop/r0-terminal",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "node build.mjs",
    "check": "tsc --noEmit",
    "test": "pnpm build && node --test test/*.test.mjs",
    "benchmark": "pnpm build && electron . --benchmark"
  },
  "dependencies": {
    "@xterm/addon-webgl": "0.19.0",
    "@xterm/xterm": "6.0.0"
  },
  "devDependencies": {
    "@types/node": "26.1.2",
    "electron": "43.3.0",
    "esbuild": "0.28.1",
    "typescript": "7.0.2"
  }
}
