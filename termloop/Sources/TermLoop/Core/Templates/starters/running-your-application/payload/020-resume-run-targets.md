---
title: "Resume run targets"
description: "MCP hint for reading already reported running targets before publishing new ones."
enabled: true
mcpTool: "get_run_targets"
includeInSkillFooter: true
---

When resuming an in-progress workspace, call `mcp__termloop__get_run_targets` to see what's already reported as running before re-publishing or starting anything new.

