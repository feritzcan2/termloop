interface:
  display_name: "cmux Release"
  short_description: "Prepare and publish termloop releases from version bump to tag."
  default_prompt: "Use this skill to run the termloop release workflow: choose version, generate user-facing changelog updates, bump versions, open and monitor release PRs, merge, tag, and verify release artifacts."
