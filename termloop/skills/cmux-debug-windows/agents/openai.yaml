interface:
  display_name: "cmux Debug Windows"
  short_description: "Tune termloop debug windows and copy snapshots."
  default_prompt: "Use this skill to manage termloop sidebar/background/menu-bar debug windows, keep Debug menu wiring clean, and capture copyable debug snapshots."
