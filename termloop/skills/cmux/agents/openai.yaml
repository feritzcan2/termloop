interface:
  display_name: "cmux Core"
  short_description: "Control windows/workspaces/panes/surfaces and routing with termloop CLI."
  default_prompt: "Use this skill to inspect and manipulate termloop topology: identify context, target handles, create/focus/move/reorder windows/workspaces/panes/surfaces, and use trigger-flash for visual confirmation."
